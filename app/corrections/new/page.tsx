"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {useSearchParams} from "next/navigation";import {getCluster,getRecord,listClusterCorrectionIds,getCorrection} from "@/lib/genlayer/data-source";import {useLive} from "@/lib/use-live";import {useContractWrite} from "@/lib/use-write";import {LoadingState,Unavailable} from "@/components/states";import {AccessionCard} from "@/components/accession-card";import {TransactionRail} from "@/components/transaction-rail";import type {CorrectionCase} from "@/lib/contract-types";
export default function NewCorrection(){const q=useSearchParams();const clusterId=Number(q.get("cluster")),recordId=Number(q.get("record"));const tx=useContractWrite();const [cid,setCid]=useState<number>();const[row,setRow]=useState<CorrectionCase>();const live=useLive(async()=>{if(!clusterId||!recordId)throw new Error("cluster and record query parameters are required");const[c,r]=await Promise.all([getCluster(clusterId),getRecord(recordId)]);if(c.kind!=="AVAILABLE"||r.kind!=="AVAILABLE")throw new Error("Live cluster and record are required");return{cluster:c.value,record:r.value}},[clusterId,recordId]);async function discover(){const ids=await listClusterCorrectionIds(clusterId,0,50);if(ids.kind!=="AVAILABLE")return;for(const id of [...ids.value].reverse()){const c=await getCorrection(id);if(c.kind==="AVAILABLE"&&c.value.record_id===recordId){setCid(id);setRow(c.value);return}}}async function propose(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await tx.run("propose_correction",[BigInt(clusterId),BigInt(recordId),String(f.get("url")),String(f.get("digest"))],discover)}catch{}}async function adjudicate(){if(!cid)return;try{await tx.run("adjudicate_correction",[BigInt(cid)],async()=>{const c=await getCorrection(cid);if(c.kind==="AVAILABLE")setRow(c.value)})}catch{}}if(live.loading)return <main className="page"><LoadingState label="Opening correction folio"/></main>;if(live.error||!live.data)return <main className="page"><Unavailable message={live.error??"Correction context unavailable"}/></main>;return <main className="page correction-page"><header className="folio-heading"><span className="eyebrow">VERSIONED CORRECTION</span><h1>Challenge cluster membership</h1><p>A prior merge receipt is never erased. A successful challenge changes current membership, increments the cluster version, and leaves both decisions in provenance.</p></header><div className="correction-layout"><AccessionCard record={live.data.record}/><section className="correction-note"><span>ACTIVE CLUSTER</span><h2>{live.data.cluster.canonical_label}</h2><p>Cluster {clusterId} · version {live.data.cluster.version} · {live.data.cluster.member_count} active records</p><Link href={`/entities/${clusterId}`}>Back to dossier</Link></section></div>{!cid?<form className="evidence-ticket" onSubmit={propose}><label>Public correction evidence URL<input name="url" type="url" required/></label><label>Evidence SHA-256 digest<input name="digest" required/></label><p className="form-note">DETACH_MEMBER requires affirmative contradiction across at least two identity-anchor categories and independent validator agreement. Missing evidence produces an insufficient result rather than rewriting history.</p><button className="seal-button" disabled={!tx.canWrite}>Propose correction</button></form>:<section className="decision-desk"><span className="ticket-no">CORRECTION {String(cid).padStart(5,"0")}</span><h2>{row?.decision||"Pending correction adjudication"}</h2>{row?.decision?<Link className="press-button" href={`/corrections/${cid}`}>Open correction receipt</Link>:<button className="seal-button" disabled={!tx.canWrite} onClick={adjudicate}>Run correction consensus</button>}</section>}<TransactionRail stage={tx.stage} hash={tx.hash} message={tx.message}/></main>}
